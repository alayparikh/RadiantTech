# Copy to .env and fill in. Vite only exposes vars prefixed with VITE_.
# Contact form email delivery via Web3Forms (https://web3forms.com).
# Create a free access key by entering info@radiantcontrolsystems.com there.
# Without this, the contact form runs in demo mode (no email is sent).
VITE_WEB3FORMS_ACCESS_KEY=
